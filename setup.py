import setuptools

setuptools.setup(
    name="meta_feature_extraction",
    version="0.0.1",
    description="Make meta-learning applied to process mining a package",
    packages=setuptools.find_packages(),
    python_requires='>=3',
)